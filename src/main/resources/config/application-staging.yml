# 说明：预生产/灰度环境配置（接近生产参数）
spring:
  jpa:
    show-sql: false         # 关闭SQL显示

logging:
  level:
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus