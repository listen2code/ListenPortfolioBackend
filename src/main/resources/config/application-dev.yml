# 说明：开发环境配置（本地开发调试用）
spring:
  jpa:
    show-sql: true
  output:
    ansi:
      enabled: always

logging:
  level:
    root: INFO
    org.springframework.web: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

