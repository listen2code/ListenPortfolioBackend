# 说明：生产环境配置（谨慎修改）
spring:
  jpa:
    show-sql: false         # 关闭SQL显示，保护敏感信息

logging:
  level:
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus