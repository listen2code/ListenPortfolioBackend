# ===================================================================
# Docker Compose 环境变量模板
# ===================================================================

# 数据库凭据
DB_USERNAME=root
DB_PASSWORD=Ls-88888888

# JWT 密钥（生产环境必须更换为强随机字符串，至少32字符）
JWT_SECRET=your-super-strong-secret-key-that-is-at-least-256-bits-long

# 邮件服务（Gmail 应用专用密码）
MAIL_USERNAME=listen2code@gmail.com
MAIL_PASSWORD=wlrwwiltnwvbjinn

# JVM 参数（可选，已有默认值）
# JAVA_OPTS=-Xms256m -Xmx512m -XX:+UseG1GC -XX:MaxGCPauseMillis=200
